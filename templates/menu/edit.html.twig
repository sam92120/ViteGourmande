{% extends 'base.html.twig' %}

{% block title %}Edit Menu{% endblock %}

{% block body %}
    <h1> Menu</h1>

    <div class="container mt-5 rounded-4 p-4 bg-light text-dark shadow">
        {{ include('menu/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_menu_index') }}" class="btn btn-secondary mt-3">back to list</a>

        {{ include('menu/_delete_form.html.twig') }}
    </div>


{% endblock %}
