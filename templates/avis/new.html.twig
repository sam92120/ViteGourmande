{% extends 'base.html.twig' %}


{% block title %}nouvel Avis{% endblock %}

{% block body %}
<div class="container mt-4 mb-4 p-4">
<div class="row justify-content-center">
    <div class="col-md-8 ">
        <div class="card shadow">
            <div class="card-header bg-primary text-white">
                <h2 class="mb-0">Laisser un avis</h2>
            </div>
            <div class="card-body bg-light">
             {{ include('avis/_form.html.twig') }}

 
                <a href="{{ path('app_avis_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
            </div>
        </div>
    </div>
</div>
</div>  

{% endblock %}
